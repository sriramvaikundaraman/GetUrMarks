<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.geturmarks">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".subjectwiseview"></activity>
        <activity android:name=".allsubjectview" />
        <activity android:name=".markupdate" />
        <activity android:name=".staffview" />
        <activity android:name=".staffupdate" />
        <activity android:name=".staffMain" />
        <activity android:name=".studentMain" />
        <activity android:name=".staffCreate" />
        <activity android:name=".studentCreate" />
        <activity android:name=".staffLogin" />
        <activity android:name=".studentLogin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>